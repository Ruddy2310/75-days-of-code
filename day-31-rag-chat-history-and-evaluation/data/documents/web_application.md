# RAG Web Application
A web-based RAG application separates the user interface from the retrieval pipeline. Flask receives a browser question, passes it to the retrieval engine, and renders the answer with source information.
