# Retrieval Reranking
Reranking is a second-stage retrieval technique. A fast retriever selects candidates, then a more accurate model scores the query and candidate passages together. A Cross-Encoder can improve final ranking.
