# RAG Web Application

A web-based RAG application separates the user interface from the retrieval pipeline. Flask can receive a question from a browser form, pass it to the retrieval engine, and render the answer together with source information.
