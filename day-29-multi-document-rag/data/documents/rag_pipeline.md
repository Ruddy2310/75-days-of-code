# RAG Pipeline

A practical RAG pipeline contains ingestion, chunking, embedding, indexing, retrieval, and answer generation. Metadata should be retained for every chunk so the application can report source documents. Chunk overlap helps preserve context across boundaries, while Top-K controls how many candidate chunks are retrieved.
