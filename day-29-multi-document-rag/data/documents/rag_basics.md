# Retrieval-Augmented Generation

Retrieval-Augmented Generation combines information retrieval with text generation. A retriever finds relevant knowledge from an external collection and a generator uses that context to produce an answer. RAG can make knowledge updates easier because documents can be replaced or added without retraining the language model.
