# RAG Web Application
A web RAG application separates the interface from retrieval. Flask receives a browser question, sends it through the retrieval pipeline, and renders the answer with source information.
