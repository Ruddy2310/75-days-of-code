# Retrieval Reranking
Reranking is a second-stage retrieval technique. A fast retriever first selects candidate passages, then a more accurate model scores the query and passage together. A Cross-Encoder can improve the final ranking because it directly models interactions between the query and passage.
