# RAG Web Application
A web RAG application separates the interface from retrieval. Flask receives a browser question, passes it to the retrieval engine, and renders the answer with source information.
